import React, { Component, PropTypes } from 'react'
import { View, Text } from 'react-native';

const RedBox = () => (
  <View  style={{borderWidth: 3, borderColor: 'red'}}>
    <Text> hello Im red box</Text>
  </View>
)

export default RedBox
